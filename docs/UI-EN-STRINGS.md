# UI-EN-STRINGS — Russian string inventory for `app.js` (EN-by-default conversion)

> **SUPERSEDED snapshot note (2026-09-18).** This inventory was curated against sha256 `ee2df2bf…6554`. `app.js` has since received bug fixes (migration snapshot/toast reachability, heatmap UTC keys, double-grade index skip, backup restore/export rewrite). The classification below still holds — the delta is console-only, see §5A.
> Row keys are the **exact string literal + enclosing function** (line numbers are snapshot-only; enclosing fn = nearest preceding function/module-level-const declaration, heuristic).
> Curated snapshot: sha256 `ee2df2bf8671a89d652cd8052e953d4ab626adbf410be32561493bebdbaf6554` · 5340 lines · Cyrillic literals: **85**
> CURRENT snapshot: sha256 `a5b2dcf30c40813044ed83e722b5568958c4c1dd10cb26a7b03060cdef055741` · mtime `2026-09-18T09:50:59Z` · `221066` bytes · 5456 lines · Cyrillic literals: **93** (+8, all OPT console diagnostics)
> Generated by the UI agent; static markup (index.html) is already English by default — see §1.

## 0. Summary

| Disposition | Count | Meaning |
|---|---|---|
| **EN — translate** | 0 | User-visible copy that must become English — **none remain: all resolved in the frozen snapshot** |
| **KEEP-RU — intentional** | 5 | Only ever shown in the RU guide view; keep Russian |
| **OPT — console diagnostics** | 40 | Not user-visible (devtools only) — **not applied by design** (parent-agent decision; code comments are Russian too) |
| **CODE — functional** | 46 | PoS keyword matching / Cyrillic hotkey aliases — translating BREAKS behaviour (confirmed intentional by the parent agent) |
| **NEEDS-REVIEW** | 0 | New strings that appeared after curation — listed in §5, must not be applied blindly |

Category legend (per the parent-agent contract): **(a)** static markup owned by the UI agent (already done, §1) · **(b)** generated by app.js into the DOM · **(c)** toast / confirm() / prompt() / alert() copy.

## 5A. Strings added after curation (all OPT — console diagnostics, 2026-09-18 bug fixes)

Recount with `node tools/theme/extract_ru.cjs` → **93** literals (was 85). Every new one is a
`console.log` / `console.error` diagnostic, i.e. the same "not applied by design" disposition as
the rest of OPT. **No new user-visible string appeared**, so the EN = 0 conclusion is unchanged.

| # | Enclosing fn | Literal | Disposition |
|---|---|---|---|
| 1 | `mergeCardInto` | `[merge] normalizeCard отклонил запись:` | OPT |
| 2 | `loadData` (legacy detect) | ``[load] источник «${cand.source}» — прежняя модель (schema_version=…), снимок до миграции будет записан`` | OPT |
| 3 | `migrateAppStateToSRS` | `всего сконвертировано:` / `(ядром: … + на слиянии: …)` | OPT |
| 4 | `restoreDataFromJson` | `[restore] миграция копии не удалась:` | OPT |
| 5 | `restoreDataFromJson` | `[restore] отклонено валидатором:` | OPT |
| 6 | `restoreDataFromJson` | `[restore] сбой чтения копии:` | OPT |

Verified separately: the only 5 potentially user-visible Cyrillic strings (`ctx` = `toast` /
`textContent`) all live in `setupGuideHandlers` and fire **only** in the `lang === 'ru'` branch —
the EN guide's copy button emits English toasts (`✅ Copied!`, `📋 AI prompt copied to clipboard!`,
`Failed to copy prompt to clipboard`). They stay KEEP-RU.

## 1. Static markup (category a) — already converted by the UI agent

`index.html` is English by default: nav («Инструкция»→Guide), knowledge panel (Knowledge Groups / not started yet / level 0 · shown in the same session / 1 day / 2–4 days / 7–14 days / 30 days · ceiling / "N words"), due summary, the three answer buttons (🔴 Forgot · 🟠 Hard · 🟢 Easy, titles + kbd hints), shortcut tip line, dictionary filter option (🏦 Bank (not studied)), group tabs, the whole Spelling Studio, the whole Statistics screen, the edit modal (Level · ENG→RUS / RUS→ENG, day-suffixed options, Review schedule), group-detail table head, and the AI-import prompt code block (now English, mirroring §3).
Deliberate Russian kept in markup: the RU guide view (hidden by default, toggle intact, `lang="ru"`), Cyrillic-layout hotkey aliases (kbd Ы/Ф/В/Ц) in the EN cheatsheet, `lang="ru"` cross-references to old button names, and Russian sample/placeholder word data (translation fields expect Russian).

## 2. EN — user-visible Russian that must be translated

**None remain.** Earlier snapshots listed ~20 user-visible rows (migration/save toasts, POS_CATEGORIES labels, the `Single Additions (Отдельные слова)` batch name, the Russian `promptText` clipboard template); the parent agent's app.js rewrite absorbed all of them before the freeze. The prompt replacement this doc proposed lives on in §3 (RESOLVED).

| Screen | Enclosing fn | Cat | Line (snapshot) | Current literal (exact) | Proposed replacement | Already English in target? |
|---|---|---|---|---|---|---|

✅ **RESOLVED — legacy `batch_name`**: earlier builds wrote `Single Additions (Отдельные слова)` into user data; without normalisation Groups & Packs would split one logical bucket in two. The frozen snapshot adds `LEGACY_BATCH_ALIASES` + `normalizeBatchName(card)` (top of app.js), called on BOTH ingestion paths — the loadData merge branch and the restore/import path — so the legacy spelling is normalised from any source (localStorage, backup, `data/*.json`). Note: the live `data/leitner_data.json` never contained the legacy name (its batches are the four "Import …" ones + empty); the exposure was localStorage/backup only. Normalisation added anyway as cheap insurance.

## 2b. KEEP-RU — intentionally Russian (do NOT translate)

These fire only when the user switches the Guide to the Russian view; they mirror the RU markup in index.html. **Confirmed intentional by the parent agent** — the frozen snapshot keeps them wired to the RU toggle only.

| Screen | Enclosing fn | Cat | Line (snapshot) | Current literal (exact) | Proposed replacement | Already English in target? |
|---|---|---|---|---|---|---|
| Guide (RU view) | `setupGuideHandlers` | b (rendered) | 728 | `'📖 Инструкция и Руководство'` | — (set only when the RU guide view is toggled; matches index.html RU header) | no |
| Guide (RU view) | `setupGuideHandlers` | b (rendered) | 729 | `'Система Лейтнера, занесение слов через ИИ, хранение данных, выгрузка/загрузка и горячие клавиши'` | — (RU guide subtitle) | no |
| Guide (RU view) | `setupGuideHandlers` | b (rendered) | 765 | `'✅ Скопировано!'` | — (RU copy-button feedback) | no |
| Guide (RU view) | `setupGuideHandlers` | c (toast) | 767 | `'📋 Промпт для нейросети скопирован в буфер обмена!'` | — (RU prompt-copy toast) | no |
| Guide (RU view) | `setupGuideHandlers` | c (toast) | 769 | `'Не удалось скопировать промпт'` | — (RU prompt-copy failure toast) | no |

## 2c. OPT — console diagnostics (not user-visible) — NOT APPLIED BY DESIGN

devtools-only logging from the load/save/migration/train engines. **Parent-agent decision: left in Russian on purpose** (developer diagnostics, not UI; code comments are Russian too). The English proposals below were NOT applied and are kept only so a future reader does not mistake these rows for an oversight.

| Screen | Enclosing fn | Cat | Line (snapshot) | Current literal (exact) | Proposed replacement | Already English in target? |
|---|---|---|---|---|---|---|
| console · load engine | `pushCandidate` | b (console/internal) | 171 | `\`[load] источник ${source} отброшен: cards не массив или пуст\`` | `[load] source ${…} rejected: cards is not an array or is empty` | no |
| console · load engine | `pushCandidate` | b (console/internal) | 186 | `'localStorage leitner_data недоступен:'` | 'localStorage leitner_data unavailable:' | no |
| console · load engine | `pushCandidate` | b (console/internal) | 191 | `'localStorage leitner_data_backup недоступен:'` | 'localStorage leitner_data_backup unavailable:' | no |
| console · load engine | `pushCandidate` | b (console/internal) | 218 | `'[load] источники:'` | '[load] sources:' | no |
| console · load engine | `pushCandidate` | b (console/internal) | 218 | `'нет данных'` | 'no data' | no |
| console · load engine | `rankOf` | b (console/internal) | 258 | `'[load] normalizeCard отклонил запись:'` | '[load] normalizeCard rejected a record:' | no |
| console · load engine | `rankOf` | b (console/internal) | 334 | `'[load] слиты настоящие дубликаты:'` | '[load] merged true duplicates:' | no |
| console · load engine | `rankOf` | b (console/internal) | 335 | `'[load] пропущено удалённых (надгробия):'` | '[load] skipped deleted (tombstones):' | no |
| console · load engine | `rankOf` | b (console/internal) | 336 | `'[load] пропущено битых записей:'` | '[load] skipped corrupt records:' | no |
| console · save engine | `saveData` | b (console/internal) | 359 | `'[SRS] сохранение ОТКЛОНЕНО:'` | '[SRS] save REJECTED:' | no |
| console · save engine | `saveData` | b (console/internal) | 365 | `'[SRS] предупреждения сохранения:'` | '[SRS] save warnings:' | no |
| console · save engine | `saveData` | b (console/internal) | 372 | `'[SRS] сериализация не удалась:'` | '[SRS] serialization failed:' | no |
| console · save engine | `saveData` | b (console/internal) | 393 | `'отказ'` | 'failed' | no |
| console · save engine | `saveData` | b (console/internal) | 395 | `'[save] предупреждения файлового слоя:'` | '[save] file-layer warnings:' | no |
| console · ipc | `unwrapIpcResult` | b (console/internal) | 459 | `\`[ipc] ${channel \|\| 'channel'}: файла ещё нет${res.path ? ' (' + res.path + ')' : ''}\`` | `[ipc] ${…}: no file yet${…}` | no |
| console · ipc | `unwrapIpcResult` | b (console/internal) | 461 | `\`[ipc] ${channel \|\| 'channel'} отказал:\`` | `[ipc] ${…} failed:` | no |
| console · ipc | `unwrapIpcResult` | b (console/internal) | 464 | `'[ipc] повреждённый файл изолирован:'` | '[ipc] corrupt file quarantined:' | no |
| console · parsing | `parseJsonTolerant` | b (console/internal) | 483 | `\`Не удалось разобрать ${label \|\| 'JSON'}:\`` | `Failed to parse ${…}:` | no |
| console · migration | `writePreMigrationSnapshot` | b (console/internal) | 510 | `'Не удалось сериализовать снимок до миграции:'` | 'Failed to serialize the pre-migration snapshot:' | no |
| console · migration | `writePreMigrationSnapshot` | b (console/internal) | 516 | `'localStorage-снимок до миграции не сохранён:'` | 'pre-migration localStorage snapshot not saved:' | no |
| console · save engine | `writePreMigrationSnapshot` | b (console/internal) | 527 | `'отказ'` | 'failed' | no |
| console · migration | `writePreMigrationSnapshot` | b (console/internal) | 528 | `\`(${res.bytes} байт)\`` | `(${…} bytes)` | no |
| console · migration | `writePreMigrationSnapshot` | b (console/internal) | 528 | `'(уже существовал — не перезаписан)'` | '(already existed — not overwritten)' | no |
| console · migration | `writePreMigrationSnapshot` | b (console/internal) | 528 | `'Снимок до миграции:'` | 'Pre-migration snapshot:' | no |
| console · migration | `writePreMigrationSnapshot` | b (console/internal) | 531 | `\`save-snapshot ${snap.filename} недоступен:\`` | `save-snapshot ${…} unavailable:` | no |
| console · migration | `migrateAppStateToSRS` | b (console/internal) | 552 | `'Миграция SRS провалилась — состояние не изменено:'` | 'SRS migration failed — state unchanged:' | no |
| console · migration | `migrateAppStateToSRS` | b (console/internal) | 559 | `'Миграция отклонена валидатором:'` | 'Migration rejected by the validator:' | no |
| console · migration | `migrateAppStateToSRS` | b (console/internal) | 571 | `\`[SRS] миграция ${sourceLabel \|\| 'base'} → schema ${SRS.SCHEMA_VERSION}\`` | `[SRS] migration ${…} → schema ${…}` | no |
| console · training | `resolveDirectionOverride` | b (console/internal) | 1233 | `'Неизвестное направление в переключателе:'` | 'Unknown direction in the switcher:' | no |
| console · training | `renderCurrentCard` | b (console/internal) | 1444 | `'[train] карточка исчезла из базы, пропускаем:'` | '[train] card vanished from the base, skipping:' | no |
| console · training | `submitAnswer` | b (console/internal) | 1697 | `'[answer] карточка не найдена, пропускаем запись:'` | '[answer] card not found, skipping the write:' | no |
| console · training | `submitAnswer` | b (console/internal) | 1719 | `'[answer] неизвестная оценка:'` | '[answer] unknown answer grade:' | no |
| console · training | `submitAnswer` | b (console/internal) | 1782 | `'[learn] вторая сторона добавлена в сессию:'` | '[learn] second side added to the session:' | no |
| console · training | `setupEventHandlers` | b (console/internal) | 2378 | `'[save] после возврата в Банк:'` | '[save] after returning to the Bank:' | no |

## 2d. CODE — functional literals (translating breaks behaviour) — CONFIRMED

Part-of-speech keyword matching and physical Cyrillic-layout hotkey aliases. **Confirmed intentional by the parent agent; translating them WOULD break behaviour**: real user data stores `part_of_speech`/translation markers as «сущ.»/«глагол»/«прил.» strings that these keywords match against, and `ы/ф/в/ц` are the same physical keys as S/A/D/W on a Russian keyboard layout. Leave exactly as they are.

| Screen | Enclosing fn | Cat | Line (snapshot) | Current literal (exact) | Proposed replacement | Already English in target? |
|---|---|---|---|---|---|---|
| Data layer · batch-name normalisation | `LEGACY_BATCH_ALIASES` | b (functional) | 70 | `'Single Additions (Отдельные слова)'` | — (legacy-data alias key; intentionally Russian — it matches old user records, translating it breaks normalisation) | no |
| Data layer · batch-name normalisation | `LEGACY_BATCH_ALIASES` | b (functional) | 71 | `'Отдельные слова'` | — (legacy-data alias key; intentionally Russian — it matches old user records, translating it breaks normalisation) | no |
| import/parsing | `inferPartOfSpeech` | b (functional) | 130 | `'сущ'` | — (Russian PoS keyword matched against card translations) | no |
| import/parsing | `inferPartOfSpeech` | b (functional) | 130 | `'существительное'` | — (PoS keyword) | no |
| import/parsing | `inferPartOfSpeech` | b (functional) | 131 | `'гл'` | — (PoS keyword) | no |
| import/parsing | `inferPartOfSpeech` | b (functional) | 131 | `'глагол'` | — (PoS keyword) | no |
| import/parsing | `inferPartOfSpeech` | b (functional) | 131 | `'что делать'` | — (verb keyword) | no |
| import/parsing | `inferPartOfSpeech` | b (functional) | 131 | `'что сделать'` | — (verb keyword) | no |
| import/parsing | `inferPartOfSpeech` | b (functional) | 132 | `'прил'` | — (PoS keyword) | no |
| import/parsing | `inferPartOfSpeech` | b (functional) | 132 | `'прилагательное'` | — (PoS keyword) | no |
| import/parsing | `inferPartOfSpeech` | b (functional) | 133 | `'нареч'` | — (PoS keyword) | no |
| import/parsing | `inferPartOfSpeech` | b (functional) | 133 | `'наречие'` | — (PoS keyword) | no |
| import/parsing | `inferPartOfSpeech` | b (functional) | 134 | `'местоим'` | — (PoS keyword) | no |
| import/parsing | `inferPartOfSpeech` | b (functional) | 135 | `'предлог'` | — (PoS keyword) | no |
| import/parsing | `inferPartOfSpeech` | b (functional) | 136 | `'союз'` | — (PoS keyword) | no |
| import/parsing | `inferPartOfSpeech` | b (functional) | 137 | `'выражение'` | — (PoS keyword) | no |
| import/parsing | `inferPartOfSpeech` | b (functional) | 137 | `'идиома'` | — (PoS keyword) | no |
| import/parsing | `inferPartOfSpeech` | b (functional) | 137 | `'фраза'` | — (PoS keyword) | no |
| Training · hotkeys | `setupEventHandlers` | b (functional) | 2288 | `'ы'` | — (Cyrillic-layout alias for S = flip) | no |
| Training · hotkeys | `setupEventHandlers` | b (functional) | 2383 | `'ф'` | — (Cyrillic-layout alias for A = Forgot) | no |
| Training · hotkeys | `setupEventHandlers` | b (functional) | 2393 | `'в'` | — (Cyrillic-layout alias for D = Easy) | no |
| Training · hotkeys | `setupEventHandlers` | b (functional) | 2405 | `'ц'` | — (Cyrillic-layout alias for W = to end of queue) | no |
| import/parsing | `renderDictionary` | b (functional) | 2836 | `'сущ'` | — (Russian PoS keyword matched against card translations) | no |
| import/parsing | `renderDictionary` | b (functional) | 2837 | `'гл'` | — (PoS keyword) | no |
| import/parsing | `renderDictionary` | b (functional) | 2838 | `'прил'` | — (PoS keyword) | no |
| import/parsing | `renderDictionary` | b (functional) | 2839 | `'нареч'` | — (PoS keyword) | no |
| Dictionary | `renderDictionary` | b (functional) | 2840 | `'выраж'` | — (PoS keyword, badge classifier) | no |
| Dictionary | `renderDictionary` | b (functional) | 2840 | `'фраз'` | — (PoS keyword, badge classifier) | no |
| import/parsing | `POS_CATEGORIES` | b (rendered) | 3019 | `'сущ'` | — (Russian PoS keyword matched against card translations) | no |
| import/parsing | `POS_CATEGORIES` | b (rendered) | 3019 | `'существительное'` | — (PoS keyword) | no |
| import/parsing | `POS_CATEGORIES` | b (rendered) | 3020 | `'гл'` | — (PoS keyword) | no |
| import/parsing | `POS_CATEGORIES` | b (rendered) | 3020 | `'глагол'` | — (PoS keyword) | no |
| import/parsing | `POS_CATEGORIES` | b (rendered) | 3021 | `'прил'` | — (PoS keyword) | no |
| import/parsing | `POS_CATEGORIES` | b (rendered) | 3021 | `'прилагательное'` | — (PoS keyword) | no |
| import/parsing | `POS_CATEGORIES` | b (rendered) | 3022 | `'нареч'` | — (PoS keyword) | no |
| import/parsing | `POS_CATEGORIES` | b (rendered) | 3022 | `'наречие'` | — (PoS keyword) | no |
| import/parsing | `POS_CATEGORIES` | b (rendered) | 3023 | `'идиома'` | — (PoS keyword) | no |
| import/parsing | `POS_CATEGORIES` | b (rendered) | 3023 | `'фраза'` | — (PoS keyword) | no |
| Groups & Packs | `POS_CATEGORIES` | b (rendered) | 3024 | `'другое'` | — (PoS keyword) | no |
| Groups & Packs | `POS_CATEGORIES` | b (rendered) | 3024 | `'местоимение'` | — (PoS keyword) | no |
| import/parsing | `POS_CATEGORIES` | b (rendered) | 3024 | `'предлог'` | — (PoS keyword) | no |
| import/parsing | `POS_CATEGORIES` | b (rendered) | 3024 | `'союз'` | — (PoS keyword) | no |
| import/parsing | `getPosCards` | b (console/internal) | 3036 | `'гл'` | — (PoS keyword) | no |
| import/parsing | `getPosCards` | b (console/internal) | 3036 | `'нареч'` | — (PoS keyword) | no |
| import/parsing | `getPosCards` | b (console/internal) | 3036 | `'прил'` | — (PoS keyword) | no |
| import/parsing | `getPosCards` | b (console/internal) | 3036 | `'сущ'` | — (Russian PoS keyword matched against card translations) | no |

## 3. AI-import prompt (`#btn-copy-prompt` handler) — ✅ RESOLVED in the frozen snapshot

This doc originally proposed the English template below for the Russian `promptText` literal. The parent agent applied it field-for-field before the freeze (the handler now copies an English prompt; the "exact Russian translation" requirement inside the prompt is preserved on purpose — word translations are DATA, not UI). Kept here for reference:

```js
const promptText = `I want to add new English words to my flashcard app.
Build a clean JSON array of objects for them, without any extra text, markdown or explanations.
Each object must have these fields:
- "word": the English word or phrase
- "transcription": IPA transcription (e.g. "[wɜːrk]")
- "translation": accurate Russian translation
- "part_of_speech": one of "noun", "verb", "adjective", "adverb", "phrase", "other"; an array like ["noun","verb"] is allowed for universal words
- "example": a natural English example sentence
- "example_translation": Russian translation of the example
- "batch_title": (optional) theme or selection name`;
```

Field list matches the visible `<code lang="en">` block in the Add Words tab of index.html (same prompt, paragraph form) and keeps the PoS values the importer parses. The Russian prompt remains available to users in the RU guide view (`#guide-prompt-code-ru`). The button label reads "📋 Copy Prompt for Gemini", so the clipboard payload is now consistent with it.

## 4. Remaining user-visible Russian, by visibility

None — every user-visible string in the frozen snapshot is already English. (~20 rows from earlier snapshots were absorbed by the app.js rewrite; the last one, the Russian `promptText`, is resolved — see §3.)

Plus: KEEP-RU set (§2b, 5 rows) stays wired to the RU guide toggle only (confirmed); legacy batch-name issue RESOLVED (§2 note); OPT console strings intentionally Russian (§2c).

## 5. NEEDS-REVIEW (strings not present when the table was curated)

None — every Cyrillic literal in the snapshot is classified above.

