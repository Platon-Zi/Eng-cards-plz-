node_modules/
.DS_Store
Thumbs.db
*.log

# Зависимости и кэш проверочного харнесса (jsdom ставится внутрь .verify/)
.verify/node_modules/
.verify/npm-cache/
npm-cache/

# Рабочие каталоги агентов и временные копии — в репозиторий не должны попасть
.scratch/
*.bak
*.tmp-*
data/*.tmp-*

# Одноразовые снапшоты до миграции оставляем в репозитории НАМЕРЕННО:
# data/leitner_data.pre-srs.{json,js} — единственная точка отката на schema 1.
